import 'package:flutter/material.dart';

class StatusTheme {
  final Color color;
  final Color bg;
  final IconData icon;
  final List<Color> gradient;

  const StatusTheme({
    required this.color,
    required this.bg,
    required this.icon,
    required this.gradient,
  });
}
